// Login options
const LOGIN_OPTIONS = {
  IDCARD: "IDCARD", // Option for logging in using an ID card
  EMAIL: "EMAIL", // Option for logging in using an email
};

// Exporting the login options
export { LOGIN_OPTIONS };
