// Minimum and maximum values for day input
const MIN_DAY = 1;
const MAX_DAY = 31;

// Minimum and maximum values for month input
const MIN_MONTH = 1;
const MAX_MONTH = 12;

// Minimum and maximum values for year input
const MIN_YEAR = 1930;
const MAX_YEAR = 2023;

// Exporting the constants
export { MIN_DAY, MAX_DAY, MIN_MONTH, MAX_MONTH, MIN_YEAR, MAX_YEAR };
